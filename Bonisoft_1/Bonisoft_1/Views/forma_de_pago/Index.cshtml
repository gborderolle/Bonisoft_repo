@model IEnumerable<Bonisoft_1.forma_de_pago>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Forma)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comentarios)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Forma)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comentarios)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Forma_de_pago_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Forma_de_pago_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Forma_de_pago_ID })
        </td>
    </tr>
}

</table>
