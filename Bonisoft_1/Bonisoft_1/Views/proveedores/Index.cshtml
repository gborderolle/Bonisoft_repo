@model IEnumerable<Bonisoft_1.proveedor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Empresa_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comentarios)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Empresa_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comentarios)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Proveedor_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Proveedor_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Proveedor_ID })
        </td>
    </tr>
}

</table>
